name: Java CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 17
        uses: actions/setup-java@v1
        with:
          java-version: 17
          server_id: github
          server-username: Dchad99
          server-password: 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
      - name: Build with Maven
        run: mvn clean package -DattachMuleSources -Pgithub deploy